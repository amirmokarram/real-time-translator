<!--
  Tray icon (idle). Same glyph as icon.svg but with no squircle behind it and
  cropped tight to its bbox — at 16px the tile frame is pure wasted pixels, and
  Windows tray icons are conventionally glyph-only. The bars are transparent
  here, so the taskbar shows through them.

  Edit here, then regenerate: npm run icons
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="3 3 58 58" width="512" height="512">
  <defs>
    <mask id="bubble" maskUnits="userSpaceOnUse" x="0" y="0" width="64" height="64">
      <rect width="64" height="64" fill="#000"/>
      <path d="M16 6 H48 A12 12 0 0 1 60 18 V34 A12 12 0 0 1 48 46 H32 L18 58 V46 H16 A12 12 0 0 1 4 34 V18 A12 12 0 0 1 16 6 Z" fill="#fff"/>
      <rect x="15" y="19" width="8" height="14" rx="4" fill="#000"/>
      <rect x="28" y="13" width="8" height="26" rx="4" fill="#000"/>
      <rect x="41" y="17" width="8" height="18" rx="4" fill="#000"/>
    </mask>
  </defs>
  <rect width="64" height="64" fill="#fff" mask="url(#bubble)"/>
</svg>
