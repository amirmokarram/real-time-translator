---
name: phase-status
description: "Build phase tracker for the Real-Time Translator — what's done and what's next"
metadata: 
  node_type: memory
  type: project
  originSessionId: d1468163-6e3b-4140-8e02-a0b3d8eb0ee3
---

Status tracker. Durable engineering lessons are in [[gotchas-and-lessons]]; architecture in [[project-architecture]].

**DONE (all 2026-06-04):**
- **Phase 1 — Scaffold:** Angular 21 + Electron 42 wired; IPC bridge (contextBridge/preload, typed `ElectronAPI`); 6 translation providers; settings persisted to `userData/settings.json`; dark dual-pane UI.
- **Phase 2 — System audio capture:** `AudioService` does `getUserMedia` desktop-loopback in renderer; `AudioContext`+`AnalyserNode` RMS level meter. (See gotchas: video-track discard.)
  - **Source picker (2026-06-04):** the audio-bar ComboBox now offers **System Audio** + **each microphone** (not per-screen rows). `AudioSource` has a `kind` ('system'|'microphone'); ids are prefixed `system:<screenId>` / `mic:<deviceId>`. `getSources()` (main) returns one System Audio entry; mics enumerated in renderer via `enumerateDevices` (with a getUserMedia probe so labels populate); `acquireStream()` branches on `kind`. NOTE: `settings.audio.selectedSourceId` gap closed 2026-07-17 (see post-roadmap polish) — selection now persists across restarts.
- **Phase 3 — STT (DeepGram):** `TranscriptionService` streams via DeepGram WebSocket; `MediaRecorder` WebM/Opus chunks; auto-reconnect. Settings has DeepGram key + Test Connection. (Free tier 12k min/yr, key at console.deepgram.com.)
  - **Sentence segmentation (2026-06-06):** DeepGram segments on *silence*, not grammar, so naive per-`is_final` commits fragmented sentences across rows. Now: buffer `is_final` fragments in `pendingFinal`, peel off complete sentences (`.!?`) via `drainSentences()` the moment the next begins → one row = one grammatical sentence, committed in real time while listening continues. Trailing partial joins the next utterance or commits via a 4s idle timer / on stop. `endpointing=800` (brief clause pauses don't split); `utterance_end_ms=1000` UtteranceEnd backstop. **Coalescing gotcha:** committed sentences go through a `pendingSentences` queue + `finalVersion` counter (NOT a single value signal) — a burst committed in one tick would otherwise be lost to Angular signal coalescing. Translator drains the queue **sequentially** (`drainSttQueue`) → one row each, in order, no overlapping `translate()` calls. `lastFinalText` now exists only as a live-panel display fallback.
- **Phase 3.5 — Meeting UX:** chat-style history (newest at bottom, auto-scroll, synced EN|FA rows); live panel replaces textarea while capturing; tabbed settings (Translation / Speech / Display).
- **LibreTranslate fix:** Content-Length + error-field handling (see gotchas).
- **Phase 4a — Overlay mode:** floating always-on-top transparent subtitle window at `#/overlay`; translation events BROADCAST to all windows from main; click-through with hover-to-wake toolbar; header toggle button.
- **Phase 4b — History export:** `ExportService` → TXT transcript or SRT subtitles via native save dialog (`export:save` IPC); export popover in audio bar.
- **Phase 5 — Assist mode (2026-06-07, branch `feature/assist-mode`):** LLM Q&A *about the conversation*, kept separate from translation. **Row selection layer** in translator (click/shift-click → highlight; per-row hover copy retained). Selection bar is **Ask-only** → opens a **slide-in panel** (lives in app shell `app.ts`, so it works on any route, not just translator; excluded from overlay window). Context sent to the LLM is **English-only** (source of truth). Panel: streaming chat, quick actions (Summarize/Explain/Key terms) when context present, collapsible context chip, multi-turn follow-ups, new-thread/close. **Header "Assist" button** opens it context-free for free-form chat. Backend mirrors translation: `electron/assist/` — `IAssistProvider` + `composeAssistPrompt` (in `electron/prompts.ts`), registry, `assist:ask` IPC streaming `assist:chunk`/`assist:complete` **to the calling window only** (not broadcast). **4 providers:** Claude + OpenAI (reuse the matching translation provider's API key — nothing to re-enter), **Ollama** (local `/api/chat`, newline-delimited JSON), **OpenAI-compatible** (generic `/chat/completions` for **Docker Model Runner**, LM Studio, vLLM, llama.cpp, LocalAI). New `assist: { provider, model, endpoint }` settings section (endpoint only for local; defaults seeded per-provider on switch). **Settings → Assist** tab: provider/model selects, local providers get free-text model + endpoint + setup how-to, **Test Connection** (`assist:validate` does a minimal ping), API-key-missing warning for cloud. `AssistService` (renderer) holds thread/streaming/context signals.
  - **Gotcha (fixed):** native `<select>` with `[value]` does NOT reliably reflect the selected option when options render as the DOM is (re)created (tab switch / app reload) → dropdown visually resets to first item though the saved value is correct. Fix: use `[ngModel]` + `[ngModelOptions]="{standalone:true}"` (no wrapping `<form>`). Applies to all settings selects.
  - **Prompt customization:** assist + translation system prompts editable in Settings (Assist / Translation tabs) with Reset-to-default; stored in `prompts:{assist,translation}` (empty → built-in default). `electron/prompts.ts` is the single source: `DEFAULT_*` consts, `composeAssistPrompt(base,context)`, `resolveTranslationPrompt(custom)` — **replaced the old `buildSystemPrompt`**. `AssistRequest`/`TranslationRequest` carry `systemPrompt`; all 4 assist providers + Claude/OpenAI translation read it (translation prompt only affects LLM providers — MT ones ignore it). `prompts:get-defaults` IPC backs editor prefill/reset.
  - **Default assist prompt = interview assistant:** English-only (no Persian unless asked), A2–B1, human/conversational. With transcript context → strict **two-section scannable Markdown**: `### 🔵 What they mean` + `### 🟢 Say this` (the read-aloud answer). Quick-action buttons retuned to **Explain / Answer / Key terms**.
  - **Free-chat vs interview:** `composeAssistPrompt` is context-aware — header Assist (no context) appends a "free chat, drop the two-section format, answer directly" footer; selection Ask appends the transcript so the interview format applies. Single customizable prompt; behaviour varies by context presence.
  - **Markdown rendering:** assistant replies + live stream go through `MarkdownPipe` (`marked` + `DOMPurify` sanitize → `SafeHtml`); user messages stay plain. Styled via `:host ::ng-deep .markdown` (innerHTML nodes carry no encapsulation attrs). **All links open in the system browser:** DOMPurify hook stamps `target=_blank`/`rel`, and main's `openLinksExternally` (`setWindowOpenHandler` + `will-navigate` → `shell.openExternal`, deny in-place nav). Deps added: `marked`, `dompurify`.
  - **Punctuation normalization:** translation IPC handler `collapseTerminalPunctuation` collapses duplicated terminal marks (؟؟/??/!! → one, prefers Persian ؟) — fixes LibreTranslate/Argos NMT doubling the final `?` on questions.

**DONE — Whisper STT provider (built 2026-06-18, merged to `main` 2026-06-24):** 2nd Speech-Recognition engine alongside DeepGram, **streaming** via local **WhisperLive** WebSocket (renderer-side, mirrors DeepGram). All 4 phases: A+B (`bdc56fe`: settings/UI/CSP + `ISttStream` strategy refactor), C+D (`fa3f432`: WhisperLive PCM stream + provider branch + ready-timeout + docs), plus GPU-image CUDA fix (`4385357`). **Merged to `main` (ff), pushed, branch deleted. Tested live & working** on RTX 3070 (8 GB) with `large-v3`. Full design + protocol in [[whisper-stt-provider]].

**DONE — Settings UX redesign (2026-06-24):** replaced the **top tab bar** (Translation/Speech/Assist/Display) with a **left-panel tree + content pane** in `src/app/features/settings/`. Tree leaves: **General** (renamed from Display), **Translation → Providers / System Prompt**, **Speech Recognition**, **Assist → Provider & Model / System Prompt**. Renderer-only (3 files: `settings.ts`/`.html`/`.scss`), no service/IPC/model changes. Key changes: `activeTab` signal → `activeNode` (tree-leaf union type); Translation no longer lists all 7 providers as an accordion — a single **provider combo-box** loads just the selected one's config, and `onTranslationProviderChange()` calls `setActiveProvider()` so picking it there **also makes it the active provider** (header dropdown stays in sync via the shared `settings()` signal). Accordion `expanded`/`toggleExpand` dropped; system prompts pulled into their own leaves. The LLM-only hint on the translation prompt is kept verbatim.

**DONE — Latency tuning, Phase A (2026-06-25):** user-facing knobs to make translations appear sooner (the pipeline was structurally ~one phrase behind the speaker). Four new `stt` settings (defaults = the old hardcoded values, so behaviour is unchanged until tuned): `endpointingMs` (DeepGram silence-before-final, was 800), `utteranceEndMs` (DeepGram UtteranceEnd backstop, was 1000, clamped ≥1000 by the API), `sentenceMaxWaitMs` (idle-commit fallback for un-punctuated tails, was the `SENTENCE_MAX_WAIT_MS=4000` static), `commitOnClause` (also split rows on `, ; :` not just `.!?`). Wiring: `endpointingMs`/`utteranceEndMs` flow through `SttStartOptions` → DeepGram URL params; `sentenceMaxWaitMs`/`commitOnClause` read in `TranscriptionService.start()` → drive the idle timer + segmentation regexes (built dynamically via `buildSentenceRe`/`buildEndsRe`). Schema added in BOTH `electron/settings-store.ts` (defaults+merge) and renderer `app.models.ts` (+ the `mockSettings` fallback in `electron-bridge.service.ts` — easy to miss, breaks `tsc`). UI: a **"Latency tuning"** subsection in Settings → Speech Recognition (number fields + clause toggle; DeepGram-only knobs hidden when Whisper selected). Whisper's pacing is server-side VAD + model size, so only `sentenceMaxWaitMs`/`commitOnClause` affect it. **Dev-run gotcha:** launching Electron via the agent's background-command harness gets the child processes reaped when the launcher returns (and `npm run electron:dev`'s `concurrently -k` then SIGTERMs `ng serve`). Run `ng serve` and Electron as **separate** background tasks, and launch Electron **detached** (`Start-Process node_modules\electron\dist\electron.exe . -WorkingDirectory <repo>` with `$env:ELECTRON_DEV='true'`) so it survives outside the harness process tree.

**DONE — Latency tuning, Phase B / live partial translation (2026-06-25, opt-in default off):** while the speaker is mid-sentence, a live Persian preview is shown that revises as more words arrive, then the normal committed row supersedes it on sentence finalize. Two new `stt` settings: `livePartial` (toggle) + `partialDebounceMs` (idle ms after the last word before re-translating the preview, default 600). **Main process:** a dedicated `translation:translate-partial` IPC that deliberately does **NOT** broadcast (overlay stays on committed rows only — no flicker) and does **NOT** stream chunks (so it can't contaminate a concurrent committed `translate()`'s `translation:chunk` subscription); otherwise mirrors the translate handler (same provider resolution + `collapseTerminalPunctuation`). Preload `translatePartial` + `ElectronAPI`/bridge method + `mockSettings`. **Renderer:** `TranslationService.livePersian` signal + `translatePartial()` guarded by a monotonic `partialGen` (a slow response is dropped if a newer partial or a commit via `clearLivePartial()` has since run) + `clearLivePartial()`. Translator: an `effect` on `transcription.interimText()` → `schedulePartial()` debounces (skips when off / not capturing / <2 chars / unchanged); cleared on commit (in the `finalVersion` effect), on stop-capture, and `ngOnDestroy`. **UI:** live panel gains a 2nd EN|FA cell (`.live-cell-fa`, RTL) shown only when `livePartial`; Settings → Speech Recognition gets a "Live preview" subsection (toggle + debounce field, field shown only when enabled). **Cost note:** with a cloud translation provider this fires ~1 request per debounce during continuous speech — local providers (Ollama/LibreTranslate) are the cheap way to run it hot.

**DONE — Playwright E2E test suite (2026-06-25, branch `feature/e2e-playwright`, commit `41b092e`, NOT pushed):** first automated tests in the repo. Drives the **built** Electron app + real Angular renderer through the real IPC bridge; fakes only the non-deterministic leaves behind guarded seams inert in normal dev/prod. Full design in [[e2e-testing]]. Seams: `MockSttStream` (renderer, `stt.provider==='mock'`, driven by `window` `e2e-stt` CustomEvents — the transcript seam must be renderer-side because STT/audio run there), `EchoProvider`/`EchoAssistProvider` (main, registered only when `process.env.TRANSLATOR_E2E`), `export:save`→temp-file bypass, per-test temp `userData` (`--user-data-dir`) seeded with `settings.json`, Chromium fake-media flags for headless `getUserMedia` + a microphone source. **18 tests green** across 6 suites (app-shell, translation-pipeline, settings persistence, TXT/SRT export, overlay mirroring, assist) + an opt-in **manual session** (`npm run e2e:manual`, `E2E_MANUAL=1`, `page.pause()` + `window.__e2e` console helpers to feed transcripts by hand). Scripts: `e2e`/`e2e:only`/`e2e:headed`/`e2e:ui`/`e2e:manual`. CI `.github/workflows/e2e.yml` (ubuntu, `playwright install-deps chromium` + xvfb, build, `xvfb-run npm run e2e:only`, uploads HTML report) — **pushed & verified GREEN** (PR #2, run `28164747036`, 18 passed). Two CI-only gotchas fixed: `npm ci` EUSAGE (regen lock with npm 11.13.0) + Electron binary postinstall truncates on the runner → curl+unzip the release zip instead of `@electron/get` (see [[gotchas-and-lessons]]). Also bumped `anyComponentStyle` CSS budget 12→16 kB. **Branch `feature/e2e-playwright` pushed, PR #2 open & green** (commits `41b092e` suite → docs sync → budget → lock fix → 3 CI fixes).

**DONE — Configurable language pair (2026-06-26):** the direction is no longer hardwired English→Persian. New `settings.languages.{source,target}` (ISO-639-1) is the single source of truth, picked via a new **Settings → Languages** leaf (curated dropdowns from a language catalog: `src/app/core/models/languages.ts` renderer + `electron/languages.ts` main — ~20 langs with native name/flag/`rtl`). `source` drives STT (`TranscriptionService.start()` now reads it — the old `stt.language` field, which was **never actually wired**, was removed), `source`+`target` drive translation. **MT providers** (Google/DeepL/Microsoft/Libre) use `request.sourceLang`/`targetLang` via `toProviderCode()` (DeepL uppercases); **LLM providers** (Claude/OpenAI/Ollama) build the prompt from `request.sourceLangName`/`targetLangName`. UI headers + per-cell direction/font (`.rtl-text`) are catalog-driven; overlay too. `TranslationEntry.english/persian` → `source/target`. **No per-provider language filtering** — unsupported pairs surface the provider's own error (chosen for simplicity). **Prompt-freeze bug found & fixed same day** (reported as "changing language doesn't work" — user was on Ollama, target=`ar`): stored default prompts were freezing the language. The auto-seeded `providers.ollama.prompt` AND a stale global `prompts.translation` both hardcoded English→Persian and won the precedence order. Fixes: Ollama seed removed + lean language-aware fallback (`resolveOllamaTranslationPrompt`); Settings Save/Reset stores `''` (not the rendered default) so defaults resolve live; `SettingsStore.migratePrompts()` (on load) clears stored prompts exactly matching a known legacy en→fa default (`LEGACY_TRANSLATION_PROMPTS`). **Lesson:** "empty = use live default" is the only freeze-safe way to persist a configurable default. e2e updated (`.target-text` selector, language-name export labels, reset-stores-empty), **18/18 green**. Not yet committed.

**DONE — Question Bank (2026-07-16, Amir confirmed good, committed & pushed to `main` incl. E2E panel coverage + README, latest `27280aa`):** assist-panel "Query From Q Bank" — LLM router picks which prepared markdown Q&A file (local folder, Settings → General) answers the selected interviewer question; match → clickable card opens the file (workflow done), no match → generates an interview-ready answer (prompt resolved in main: live-read file → Settings editor → distilled default). Full design + decision history in [[question-bank]].

**DONE — System tray + close-to-tray (2026-07-16, Amir tested good; Phase 1 of the 3-phase tray/hotkeys plan):** `electron/tray.ts` `TrayManager` (mirrors OverlayManager pattern) — icon reuses `public/favicon.ico` (dev: `dist-electron/../public`; packaged: `process.resourcesPath/public` via existing extraResources), state-aware menu **Show/Hide Window · Start/Stop Capture · Show/Hide Overlay · Quit** rebuilt via `refresh()`, double-click → show+focus. **Close-to-tray:** new `settings.tray.closeToTray` (default **true**; Settings → General "Keep Running in Tray" toggle) — window `close` intercepted → `hide()` unless `isQuitting` (set in `before-quit`, so tray Quit + OS shutdown pass through). `AudioCapture` instance moved from ipc-handlers module-level into `main.ts` and passed in (shared with tray so the menu tracks `isActive()`); capture/overlay/window show-hide all `refresh()` the menu. Capture toggle from tray = `command:toggle-capture` webContents event → preload `onToggleCaptureCommand` → new root `CommandService` (init in `app.ts`, main window only) → AudioService start/stop + `clearLivePartial()` + lazy `loadSources()` (capture is renderer getUserMedia, can't run in main). **E2E guard:** `TRANSLATOR_E2E` skips tray creation AND close-to-tray interception (Playwright must really close the app; CI has no tray).

**DONE — Global hotkeys + Settings UI (2026-07-16, Amir tested good; Phases 2+3 of the tray/hotkeys plan — FINAL planned feature, roadmap complete):** `electron/hotkeys.ts` `HotkeyManager` — registers `settings.hotkeys` `{toggleCapture:'Ctrl+Alt+C', toggleOverlay:'Ctrl+Alt+O', showHideWindow:'Ctrl+Alt+H'}` ('' = disabled) as `globalShortcut`s; idempotent `apply()` (unregisterAll→register; taken/invalid combos warn+skip, never crash); actions reuse tray helpers (`TrayManager.toggleCapture`/`toggleMainWindow`, `overlayManager.toggle`); applied at startup + **re-applied on every `settings:save`** (live rebind, no restart); `will-quit` → unregisterAll; **registers nothing under TRANSLATOR_E2E** (CI must not grab system keys). **Settings → Hotkeys panel** (new `'hotkeys'` tree leaf under General): per-action key-recorder input — click → capture next keydown via `event.code` (layout-proof), requires ≥1 modifier, Backspace/Delete = disable, Esc/blur = cancel, per-row Clear; duplicate combos show warning + disable Save; Save flashes "Saved — active immediately". `updateHotkeys` in SettingsService; `mockSettings` updated (tsc breaks if missed). **E2E:** 3 tests in settings.spec.ts (defaults from merge, record Ctrl+Shift+F9 → persists, Backspace → '', duplicate blocks Save) — suite 28/28 green.

**No features remaining — original roadmap fully shipped.** Future work = new ideas only (see "Ideas discussed, NOT built" below).

**DONE — Post-roadmap polish (2026-07-17, Amir tested good):** the leftover idea-list items. **(1) Assist stop-generation:** while streaming, the send button becomes a Stop square → keeps the partial text as the answer and detaches. Assist stream IPC events now carry `{ requestId, text }` (was bare string — `assist:chunk`/`assist:complete` payload shape changed in ipc-handlers/preload/`AssistStreamEvent` model): `AssistService.activeRequestId` filters chunks so a stopped generation's late chunks/completion are dropped even if a new ask starts immediately. Soft stop only — main finishes generating in the background (a real abort would need cancellation through all 4 providers; deliberately skipped). **(2) Assist copy-answer:** hover button on assistant bubbles, copies raw markdown, ✓ flash (`copiedIdx`). **(3) Persistent audio source:** `settings.audio.selectedSourceId` finally wired (the long-standing gap from Phase 2) — `AudioService.selectSource` persists; `loadSources` restores when the id still exists, else falls back to System Audio (mic deviceIds change across replugs). E2E suite still 28/28. **NOT done:** README screenshots (needs real PNGs from Amir).

**DONE — Packaging + CI release pipeline (2026-06-07):** Windows installer now built & published by **GitHub Actions**, not locally. `.github/workflows/release.yml`: triggers on `v*` tag push → `windows-latest` → `npm ci` → `npm run electron:build && npx electron-builder --win --publish always` (uses built-in `GITHUB_TOKEN`, `permissions: contents: write`). `electron-builder.json` has a `publish` block (provider github, owner `amirmokarram`, repo `real-time-translator`, `releaseType: draft`) → creates a **draft** release with `Setup.exe` + `.blockmap` + `latest.yml` (auto-update manifest); Amir publishes manually. **To cut a release:** `npm version <x.y.z>` then `git push --follow-tags`. First successful run = run `27103166312`, draft release `v1.0.0`. Three gotchas hit & fixed: (1) `npm ci` EUSAGE — lock file from older npm omitted `@emnapi/core`/`@emnapi/runtime` optional deps the runner's npm 11.13.0 requires; fix `npx -y npm@11.13.0 install --package-lock-only`. (2) electron-builder rejects `public/favicon.ico` <256×256 — replaced 16×16 with a 256×256 PNG-backed `.ico`. (3) **Local** `npm run electron:dist:win` fails extracting winCodeSign macOS `.dylib` symlinks unless Windows **Developer Mode** is ON (or admin shell) — CI runner unaffected; clear stale `%LOCALAPPDATA%\electron-builder\Cache\winCodeSign` after a failed extract. Actions pinned to `@v5` (checkout/setup-node) — v4 ran on deprecated Node 20.

**Git:** repo on `main`. Commits: `ba00580` (initial, Phases 1–4a) → `8a6af5a` (history export) → `b5389c0` (CLAUDE.md + docs/memory) → `de90074` (System Audio + mic source picker) → `7f88d3c` (memory) → `499a675` (tsconfig rootDir) → `552b695` (per-row copy-English button) → `306d242` (drop Live-panel idle placeholder) → `d4bc404` (fixed --composer-height: Live/Translate panels same height) → `42b4ff8` (STT per-sentence segmentation). `.gitattributes` normalizes LF; `dist-electron`/`release` gitignored.
- **Phase 5 `feature/assist-mode`** — **merged into `main` (no-ff `7ccc3e3`) and branch deleted (2026-06-07).** Commits: `6809e23` (core assist: Ask on selected rows) → `a3ee6d1` (Assist settings tab) → `3a00ae2` (header Assist button, panel→app shell) → `c7de1b2` (local providers: Ollama + OpenAI-compatible/DMR) → `80cbd31` (Test Connection + `[ngModel]` select fix) → `5d957ce` (memory) → `fd1ce66` (customizable prompts + interview default) → `162fbd9` (markdown render + external links) → `c28fb68` (two-section prompt) → `badc0ce` (punctuation normalize) → `9d64113` (free-chat mode) → memory. Also bumped `anyComponentStyle` CSS budget (8→12kB) for grown translator styles.

**PUBLISHED to GitHub (2026-06-07):** public repo **https://github.com/amirmokarram/real-time-translator**, `origin` remote configured, `main` pushed and in sync. **Proprietary licensing — All Rights Reserved (NOT open source):** `LICENSE` = "Copyright (c) 2026 Amir Mokarram. All Rights Reserved." (public for reference/demo only, no use/copy/modify/distribute permission granted); `package.json` has `"author": "Amir Mokarram"`, `"license": "UNLICENSED"`. `README.md` rewritten (`eee9cad`) from Angular CLI boilerplate → full project docs (features, tech stack, architecture tree, setup, usage, build/package, license). Tooling: `gh` CLI v2.93 installed via `winget` (at `C:\Program Files\GitHub CLI\gh.exe`); Amir authenticated as `amirmokarram` (he ran `gh auth login` + created the empty repo himself). **Note:** gh is on the Machine PATH but tool shell sessions may need `$env:Path` refreshed. **Optional follow-up (not done):** README Screenshots section — needs actual PNGs from Amir.

**Smaller UX done (2026-06-06):** per-row hover **copy-English** button (`copiedId` signal, `navigator.clipboard`); bottom composer pinned to `--composer-height: 96px` so Live↔Translate swap doesn't shift UI; removed Persian "در انتظار صدا…" idle placeholder.

**Assist mode + multi-select rows: BUILT** — see Phase 5 above (was previously the top "ideas, not built" item). Both shipped on `feature/assist-mode`.

**Ideas discussed, NOT built:** assist reset-thread-on-close; README Screenshots section (needs actual PNGs from Amir); real mid-request assist abort (cancellation through all 4 providers — soft stop shipped instead). Everything else from the old idea list shipped (stop-generation + copy-answer 2026-07-17; markdown rendering earlier).
