# Dependency Injection

What is dependency injection and why does it matter?

DI inverts the ownership of dependencies so components receive collaborators
instead of constructing them — testable seams by design.
